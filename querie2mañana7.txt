SELECT actor
FROM casts  
GROUP BY actor
HAVING COUNT(title) IN (SELECT (movie_title) FROM movies WHERE country= 'usa') 
ORDER BY COUNT(title) DESC;

(SELECT movie_title
FROM movies
WHERE country = 'usa')

(
SELECT actor
FROM CASTS
JOIN movies
ON (MOVIES.movie_title = CASTS.title)
WHERE (COUNT(movie_title) FROM movies WHERE country = 'usa') AND (ROWNUM <= 5)
ORDER BY COUNT(movie_title) DESC;


-----------------------------------------------
SELECT actor
FROM casts
WHERE (COUNT (title) IN (SELECT (movie_title) FROM movies WHERE country='usa'))
GROUP BY actor
ORDER BY COUNT (title) DESC;
