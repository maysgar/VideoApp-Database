SELECT actor
FROM (SELECT actor
FROM (SELECT actor, COUNT(title) AS cuenta
FROM (SELECT actor, title
FROM (SELECT actor, title, country
FROM casts
JOIN movies
ON (MOVIES.movie_title = CASTS.title))
WHERE country = 'USA')
GROUP BY actor
ORDER BY cuenta DESC))
WHERE ROWNUM <= 5;
